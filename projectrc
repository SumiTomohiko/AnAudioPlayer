
#cd into the home dir - this way it works when run from inside vim or any other folder
cd $(dirname $0)

dirpath=$(pwd)
src_dir="${dirpath}/src"
libs_dir="${dirpath}/libs"
app_name="anaudioplayer"
pkg_dir="jp/ddo/neko_daisuki/${app_name}"
r_dir="${src_dir}/${pkg_dir}"
build_dir="${dirpath}/_build"
classes_dir="${build_dir}/classes"
javac_opt="-verbose"
dex_path="${build_dir}/${app_name}.dex"
dist_dir="${dirpath}/dist"

if [ -f projectrc.local ]; then
	. projectrc.local
fi

# vim: filetype=sh
